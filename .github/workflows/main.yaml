name: Deploy ML Training Image

# on:
#   workflow_dispatch:
#   push:
#     branches: [main]
#     paths-ignore:
#       - 'README.md'

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]

jobs:
  deploy-staging:
    uses: viam-modules/common-workflows/.github/workflows/deploy_training_script.yaml@init
    with:
      framework: tflite
      script_name: classification-tflite
    secrets: inherit
